using System;
using FirstPokerTry.Logics.CardFactory.Classes;
using FirstPokerTry.Logics.Objects;
namespace FirstPokerTry.Logics.Gameplay
{
	public class CheckWinner
	{/*
        private Player _winner;

        public int checkCards(List<CardObject> hand)
        {
            int score = 0;
            var handChecker = new HandChecker();
            handChecker.checkIfPairExists(hand);

            if(handChecker.checkIfPairExists(hand))
            {
                score = 1;
            } else if (handChecker.checkIfTwoPairsExists(hand))
            {
                score = 2;
            } else if (handChecker.checkifThreeOfAKindExists(hand))
            {
                score = 3;
            } else if (handChecker.checkIfStraighExists(hand))
            {
                score = 4;
            } else if (handChecker.checkIfFlushExists(hand))
            {
                score = 5;
            } else if (handChecker.checkIfFullHouseExists(hand))
            {
                score = 6;
            } else if (handChecker.checkifFourOfAKindExists(hand))
            {
                score = 7;
            } else if (handChecker.checkIfStraightFlushExists(hand))
            {
                score = 8;
            } else if (handChecker.checkIfRoyalFlushExits(hand))
            {
                score = 9;
            }


            return score;
        }

        public Player findWinner(int score)
        {
            int i = score;
            

            switch (i)
            {
                case 
            }
        }*/
	}
}

